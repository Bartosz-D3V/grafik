query getRocketResults($limit: Int){
    rocketsResult(limit: $limit) {
        result {
            totalCount
        }
        data {
            name
            country
            total_per_launch
        }
    }
}
